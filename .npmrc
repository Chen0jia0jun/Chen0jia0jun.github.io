; Removed project-level prefix/global settings to avoid CI failures.
; If you need a custom npm prefix for local development, set it in your
; user-level ~/.npmrc instead (not checked into the repo).

